# Tab 6

Here is the note:

\---

\*\*NOTE: False Root Splits\*\*

The tree does not force a binary split at the root of Entity.

\*\*Independent/dependent\*\* — rejected as a type split.  
\- A truck is independent. A trailer is dependent.  
\- Both are vehicles. Dependency is a \*relationship attribute\*, not a governing constraint.  
\- Applying this as a root split would place truck and trailer under different top-level categories despite both being vehicles. This violates the governing constraint rule.

\*\*Concrete/abstract\*\* — rejected as a type split.  
\- Container includes both concrete containers (box, bag) and abstract containers (team, database).  
\- Concrete vs. abstract is a \*mode of existence attribute\*, not a governing constraint that defines what a container \*is\*.  
\- Applying this as a root split would split container across two top-level categories, forcing a false choice.

\*\*Principle:\*\*

A root split at Entity must be based on a single governing constraint that applies to \*all\* entities and produces non-overlapping, real kinds. No such constraint has been found.

Therefore:

\- Entity has no forced level-two split.  
\- Subtypes emerge through domain-specific array analysis.  
\- Attributes (independent/dependent, concrete/abstract, etc.) are documented separately and apply across the tree.  
\- The tree remains defensible because every node is placed according to its governing constraint, not by forced binary classifications.

\---

This preserves the completeness rule—every kind finds its home—without imposing false categories at the root.
