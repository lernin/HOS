name: Ocean Estate art review
on:
  push:
    branches: [feature/ocean-estate-art-studio-20260911]
    paths:
      - src/experiences/estate/**
      - public/materials-studio/**
      - tests/ocean-estate*
      - .github/workflows/ocean-estate-art-review.yml
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - run: node --test tests/ocean-estate.test.mjs tests/waterfall-village.test.mjs
      - name: Verify art catalog surface
        run: |
          grep -q "50 original curated works" public/materials-studio/art.html
          grep -q "materials-studio-art-favorites-v1" public/materials-studio/art.html
          grep -q "decorateArt" src/experiences/estate/scene.ts
      - run: npm install --no-save --package-lock=false playwright@1.55.0
      - run: npx playwright install --with-deps chromium
      - name: Render estate and verify phone UI
        run: |
          npm run dev -- --port 4173 > /tmp/ocean-estate-vite.log 2>&1 &
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:4173/tests/ocean-estate-visual.html >/dev/null; then break; fi
            sleep 1
          done
          node tests/ocean-estate-visual.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: ocean-estate-art-review-${{ github.sha }}
          path: artifacts/ocean-estate/
